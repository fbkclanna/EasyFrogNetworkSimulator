from FrogSleepControl.FrogSleepControl import FSC
from FrogSleepControl.FSCsettings import FSCsettings

fsc_settings = fsc_settings()

FSC_list = []
for i in range(2):
    FSC_list.append(FSC(max_energy=100, fsc_settings=fsc_settings)

                    for t in range(1000):
    for each in FSC_list:
        each.max_energy -= 1
    each.
    print each.isActive()
    if each.isActive():
        p = P_active2inactive()
    else:
        p = P_inactive2active()
